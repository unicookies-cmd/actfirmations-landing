# Deployment Health Check

If the site looks broken:
- Confirm `styles.css` loads (no 404)
- Confirm `app.js` loads
- Confirm `cookie-messages.js` loads
- Confirm `/assets/logo.png` loads

If any fail → check filenames + paths
